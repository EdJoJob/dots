.DS_Store
.netrwhist
*.pyc
notes/
/.claude/

# nvim runtime artifacts (backup/swap/undo live in XDG state dirs, spell
# compiles are derived; guards in case anything still writes into the tree)
packages/vim/.config/nvim/spell/*.spl
