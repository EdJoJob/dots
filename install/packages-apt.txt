# apt package manifest — installed by install/packages.sh
# One package per line; # comments and blank lines are ignored.

# Python
python-is-python3
python3
python3-gpg
python3-pip
python3-venv

# Misc / build
apt-transport-https
build-essential
ca-certificates
curl
software-properties-common

# Desktop
caffeine
espeak
redshift-gtk
zeal

# Utilities
cargo
docker.io
entr
direnv
fd-find
fortune-mod
# fortune-mod: apt.sh used "fortune", which is not a Debian package name
fzf
git
git-delta
# git-delta: packaged in Debian 12+/Ubuntu 24.04+ (replaces the old pinned .deb download)
git-lfs
gpg-agent
# gpg-agent: apt.sh used the transitional "gnupg-agent", gone on Debian 12+
htop
jq
neovim
notmuch
# lieer: Debian 13+/Ubuntu 24.04+ (older releases only have 'gmailieer' 1.3;
# lieer/python tools: via mise+uvx everywhere for consistency — install/tools.sh)
pinentry-curses
ripgrep
stow
tmux
wget
zsh
eza
# eza: only in Debian 13+/Ubuntu 24.10+; on older releases remove this line or install another way

# dropped: python2 (removed from Debian 12+)

# language servers for nvim native LSP (pyright/ts/yaml/json/vim servers: not
# packaged here — installed via npm in install/tools.sh)
gopls
