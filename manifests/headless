# Headless linux (dev containers, VMs). Activated per machine via
# `echo headless > ~/.config/dots/manifest` (or DOTS_MANIFEST=headless);
# replaces the common+linux selection entirely in this repo.
bin
dircolors
direnv
git
# The client half only: no local iTerm2 here, just a reachable API.
# See docs/decisions/0011-iterm-client-package-split.md.
iterm-client
# gnupg
ssh
tmux
vim
zsh
